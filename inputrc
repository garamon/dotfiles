# ファイル名補完機能で大文字と小文字の区別をなくす
# Ignore case when doing completion.
set completion-ignore-case on

# ビープ音を無効に
# Turn off beep.
set bell-style none

# 複数の補完候補を一覧で表示
set show-all-if-ambiguous on

# 補完候補がシンボリックリンクの場合、後ろに@を付加
set mark-symlinked-directories on

set visible-stats on
